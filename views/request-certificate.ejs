<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Request Certificate - Let's Encrypt Certificate Generator</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">Let's Encrypt Certificate Generator</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/certificates/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/certificates/request">Request Certificate</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <%= user.name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/users/logout">Logout</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow-sm">
          <div class="card-body">
            <h2 class="card-title text-center mb-4">Request SSL/TLS Certificate</h2>
            
            <form id="certificate-form">
              <div class="mb-3">
                <label for="domain" class="form-label">Domain Name</label>
                <input type="text" class="form-control" id="domain" name="domain" placeholder="example.com" required>
                <div class="form-text">Enter the domain you want to secure with SSL/TLS</div>
              </div>
              
              <div class="mb-3">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>" required>
                <div class="form-text">Your email will be used for certificate expiration notifications</div>
              </div>
              
              <div class="mb-4">
                <label class="form-label">Verification Method</label>
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="challengeType" id="httpVerification" value="http" checked>
                  <label class="form-check-label" for="httpVerification">
                    HTTP Verification
                  </label>
                  <div class="form-text ms-4">
                    The application must be hosted on the domain you're requesting a certificate for.
                    Best for domains you control completely.
                  </div>
                </div>
                
                <div class="form-check mt-2">
                  <input class="form-check-input" type="radio" name="challengeType" id="dnsVerification" value="dns">
                  <label class="form-check-label" for="dnsVerification">
                    DNS Verification
                  </label>
                  <div class="form-text ms-4">
                    You'll need to add a TXT record to your domain's DNS settings.
                    Works for any domain even if you can't host this application on it.
                  </div>
                </div>
              </div>
              
              <div id="httpAlert" class="alert alert-info mb-4">
                <h5>Important: HTTP Verification</h5>
                <p>To verify domain ownership using HTTP, you'll need to:</p>
                <ol>
                  <li>Make sure this application is hosted on the domain you're requesting a certificate for</li>
                  <li>Ensure the domain is publicly accessible (not localhost)</li>
                  <li>The application will handle the verification automatically</li>
                </ol>
              </div>
              
              <div id="dnsAlert" class="alert alert-info mb-4 d-none">
                <h5>Important: DNS Verification</h5>
                <p>To verify domain ownership using DNS, you'll need to:</p>
                <ol>
                  <li>Add a TXT record to your domain's DNS settings</li>
                  <li>Wait for DNS propagation (can take up to 24 hours)</li>
                  <li>Complete the verification process</li>
                </ol>
                <p>We'll provide the exact TXT record details after you submit the form.</p>
              </div>
              
              <div class="text-center">
                <a href="/certificates/dashboard" class="btn btn-outline-secondary me-2">Back</a>
                <button type="submit" class="btn btn-primary">Generate Certificate</button>
              </div>
            </form>

            <!-- Add this status container below the form -->
            <div id="status-container" class="mt-4 p-3 border rounded min-h-100">
              <h3 class="h5 mb-3">Status</h3>
              <p class="text-muted">Submit the form to request a certificate</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <footer class="text-center mt-5 mb-3">
    <p class="text-muted">Let's Encrypt Certificate Generator © <%= new Date().getFullYear() %></p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/certificate-request.js"></script>
  <script>
    // Toggle between HTTP and DNS verification alerts
    document.addEventListener('DOMContentLoaded', function() {
      const httpRadio = document.getElementById('httpVerification');
      const dnsRadio = document.getElementById('dnsVerification');
      const httpAlert = document.getElementById('httpAlert');
      const dnsAlert = document.getElementById('dnsAlert');
      
      // Initial setup based on checked state
      if (httpRadio.checked) {
        httpAlert.classList.remove('d-none');
        dnsAlert.classList.add('d-none');
      } else if (dnsRadio.checked) {
        httpAlert.classList.add('d-none');
        dnsAlert.classList.remove('d-none');
      }
      
      httpRadio.addEventListener('change', function() {
        httpAlert.classList.remove('d-none');
        dnsAlert.classList.add('d-none');
      });
      
      dnsRadio.addEventListener('change', function() {
        httpAlert.classList.add('d-none');
        dnsAlert.classList.remove('d-none');
      });
    });
  </script>
</body>
</html> 